package com.amrdeveloper.colorschema.core

import androidx.compose.material3.ColorScheme

data class LiloColorSchema(
    val appColorSchema: ColorScheme,
    val editorSchema: EditorSchema,
    val terminalSchema: TerminalSchema
)
